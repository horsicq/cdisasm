/*
 * The ordered and unordered classic-VEX scalar compares share one encoding
 * contract and one exhaustive control-space test.  Keep both CMake targets
 * pointed at that single source of truth.
 */
#include "test_x86_vucomi.c"
